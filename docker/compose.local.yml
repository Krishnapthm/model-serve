# Local development stack — no GPU required
# Usage: docker compose -f docker/compose.local.yml up --build
#
# Use this to test the UI, model browsing, API keys, etc.
# Model serving (vLLM) is excluded — it requires a GPU.

include:
  - compose.base.yml
